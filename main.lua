-- main file
function love.load()
    -- 1. Create the world
    -- grid, a way to load sprites, a way to load maps, a way to load a camera
    local sti = require("/library/Simple-Tiled-Implementation/sti")
    local camera = require("/library/hump/camera")
    local anim8 = require("/library/anim8/anim8")
end

function love.update(dt) end

function love.draw() end
